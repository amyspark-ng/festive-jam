{
	"name": "mygame",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite",
		"preview": "vite preview",
		"zip": "bun run build && mkdir -p dist && zip -r dist/game.zip dist -x \"**/.DS_Store\"",
		"check": "tsc"
	},
	"dependencies": {
		"dprint": "^0.47.6",
		"kaplay": "^3001.0.2"
	},
	"devDependencies": {
		"@types/node": "^22.10.1",
		"typescript": "^5.7.2",
		"vite": "^6.0.3"
	}
}
